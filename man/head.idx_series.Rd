% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idx_series.R
\name{head.idx_series}
\alias{head.idx_series}
\title{First observations of an \code{idx_series}}
\usage{
\method{head}{idx_series}(x, n = 6L, ...)
}
\arguments{
\item{x}{An \code{idx_series} object.}

\item{n}{Number of observations to return from the start of \code{x}.
Default \code{6L}. If \code{n} exceeds \code{length(x)}, the full
series is returned.}

\item{...}{Unused.}
}
\value{
An \code{idx_series} of length \code{min(n, length(x))}.
}
\description{
Returns the first \code{n} observations of \code{x}, in
position order. Unlike indexing \code{x} directly with \code{[}, \code{n}
is a count of observations (as for \code{utils::head}), not a set of
absolute positions, so \code{head(x, 6)} always returns the first six
observations regardless of \code{x$start}.
}
