% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/idx_series.R
\name{as_idx_series}
\alias{as_idx_series}
\title{Coerce to \code{idx_series}}
\usage{
as_idx_series(x, start = 1L)
}
\arguments{
\item{x}{A numeric vector, matrix or \code{idx_series} object.}

\item{start}{Integer start position, only used if \code{x} is not
already an \code{idx_series}. Defaults to \code{1}.}
}
\value{
An \code{idx_series} object.
}
\description{
Coerces plain vectors/matrices to \code{idx_series}, or
passes through an \code{idx_series} unchanged. Convenience helper so
that internal functions can accept either raw data or an already
constructed \code{idx_series}.
}
